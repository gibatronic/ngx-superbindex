name: CI

on:
  - pull_request

env:
  ENVIRONMENT: production

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3.5.1
        with:
          node-version: '14'
      - run: make ci
